DROP TABLE IF EXISTS `$TABLENAME$`;
CREATE TABLE `$TABLENAME$` ($FOREACH FIELD BEGIN$
  `$FIELDNAME$` $FIELDDATATYPE$ $CHARACTERTAG$ $DEFAULTTAG$ COMMENT '$PROPERTYDESC$',$FOREACH FIELD END$
  PRIMARY KEY (`$PKFIELDNAME$`)
) COMMENT='$ENTITYDESC$';